/*
    MATH BASE

    The base functions for all math classes. The two things that are exempt from this are Variables and Numbers,
        as they are considered bases already.
*/

export default class MathBase {
    // CONSTRUCTOR
    // ARGS:
    //   - iterables: Arrays or bases(numbers, varaibles)
    constructor(...iterables) {
        
    }
}